# Development Notes

- The Flask app is set up with SQLite using Flask-SQLAlchemy.
- The machine learning logic is separated into the app/ml package.
- Replace the placeholder prediction flow with real CNN + logistic regression inference when training data becomes available.
- Run the app with: python app.py
